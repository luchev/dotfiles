local options = {
    ensure_installed = { "lua", "vim", "vimdoc", "python", "go", "markdown", "markdown_inline", "bash", "nu", "yaml", "typescript", "javascript", "rust", "proto", "git_config" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
