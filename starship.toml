add_newline = false

format = """
$directory\
$git_branch\
$git_commit\
$git_state\
$git_status\
$custom\
$jobs\
$character"""

command_timeout = 5000

[directory]
truncation_length = 2

[git_branch]
format = "[$symbol$branch]($style)"
