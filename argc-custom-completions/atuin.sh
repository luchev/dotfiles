#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts

# @flag -h --help                              Print help
# @flag -V --version                           Print version
# @flag --color[auto|never|always]             Output coloring
# @option -c --config <path>                   Config file path
# @option --data-dir <path>                    Data directory
# @option -r --record-store-path <path>        Record store path
# @arg command[setup|history|hook|import|stats|search|sync|login|logout|register|key|status|account|kv|store|dotfiles|scripts|init|info|doctor|wrapped|daemon|default-config|config|ai|mcp|pty-proxy|uuid|contributors|gen-completions]
# @arg args~

command eval "$(argc --argc-eval "$0" "$@")"
