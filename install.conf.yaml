- clean: ['~']

- link:
    ~/.profile: profile
    ~/.vimrc: vimrc
    ~/.zshrc:
        path: zshrc
        force: true
    ~/.config/nvim/init.vim:
        path: nvim
        create: true
    ~/.config/starship.toml: starship 
    ~/.tmux.conf:
        path: tmux
        force: true
